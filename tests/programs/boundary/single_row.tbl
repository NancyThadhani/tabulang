table s = load("one.csv") with schema { region : string, revenue : int, units : int };
table g = s |> derive(p = revenue / units) |> group_by(region) |> aggregate(n = count(units), avg = mean(p));
show g;
