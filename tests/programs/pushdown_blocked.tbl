table s = load("sales.csv") with schema { region : string, revenue : int, units : int };
table d = s |> derive(unit_price = revenue / units) |> filter(unit_price >= 150) |> select(region, unit_price);
show d;
